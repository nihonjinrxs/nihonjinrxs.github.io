{
  "name": "codeanddata.codes",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "cra-template": "1.0.3",
    "dompurify": "^2.2.6",
    "marked": "^1.2.2",
    "moment": "^2.29.1",
    "react": "^16.14.0",
    "react-dom": "^16.14.0",
    "react-scripts": "^4.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && rm -rf ./docs && mv ./build ./docs",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "npm run build && git add . && git commit -m 'Rebuild site' && git push origin main"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^5.11.5",
    "@testing-library/react": "^11.1.0"
  }
}
