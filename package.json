{
  "name": "codeanddata.codes",
  "version": "1.1.1",
  "private": true,
  "dependencies": {
    "cra-template": "1.2.0",
    "dompurify": "^3.1.6",
    "marked": "^14.1.0",
    "moment": "^2.30.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-toggle": "^4.1.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "npm audit --production && react-scripts build && rm -rf ./docs && mv ./build ./docs",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "npm audit --production && npm run build && git add . && git commit -m 'Rebuild site' && git push origin main"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@babel/plugin-proposal-private-property-in-object": "^7.21.11",
    "@testing-library/jest-dom": "^6.5.0",
    "@testing-library/react": "^16.0.0",
    "create-react-app": "^5.0.1",
    "react-scripts": "^5.0.1"
  }
}
